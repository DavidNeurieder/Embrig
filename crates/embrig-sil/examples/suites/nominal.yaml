# A valid temperature (10..90 °C) opens the valve.
name: valid_temperature_opens_valve
timeout: 5s
steps:
  - wait: { time: 300ms }
  - expect: { id: 0x200, signal: valve_open, equals: true, within: 1s }
